import React, { Fragment } from "react";
import Sidebar from "../Sidebar/Sidebar";

const Dashboard = () => {
  return (
    <Fragment>
      <div>
        <Sidebar />
        <h2>Dashboard</h2>
      </div>
    </Fragment>
  );
};

export default Dashboard;
